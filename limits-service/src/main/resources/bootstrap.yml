spring:
  application:
    name: limits-service
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: qa
# EUREKA CONFIGURATION
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761 # EUREKA NAMING SERVER URL